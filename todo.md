# Refactor TODOs

- [ ] Rewrite README.md to document the current `run` script instead of `stow`.
- [ ] Create an `lsp-update.sh` script to fetch/update language server binaries
manually.
- [ ] Setup a formatter for Lua files (e.g., Stylua) and integrate it like
`clang-format` for Neovim config.
- [ ] (MAYBE) distro-agnostic post-installation script
    - one main post-install script that calls distro-specific scripts??
- [x] get rid of stow
    - symlink?
    - copy?
- [x] new `run` script
- [x] better program-specific management
- [x] more rice??
