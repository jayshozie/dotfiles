#!/usr/bin/env bash

check_binaries() {
    if ! command -v mako &> /dev/null; then
        log "${ERR}[RUN] [ERROR] mako is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
