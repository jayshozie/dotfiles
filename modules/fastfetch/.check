#!/usr/bin/env bash

check_binaries() {
    if ! command -v fastfetch &> /dev/null; then
        log "${ERR}[RUN] [ERROR] fastfetch is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
