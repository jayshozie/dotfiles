#!/usr/bin/env bash

check_binaries() {
    if ! command -v git &> /dev/null; then
        log "${ERR}[RUN] [ERROR] git is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
