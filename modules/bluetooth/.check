#!/usr/bin/env bash

check_binaries() {
    if ! command -v bluetoothctl &> /dev/null; then
        log "${ERR}[RUN] [ERROR] bluez/bluetoothctl is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
