#!/usr/bin/env bash

check_binaries() {
    if ! command -v lazygit &> /dev/null; then
        log "${ERR}[RUN] [ERROR] lazygit is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
