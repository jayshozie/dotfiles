#!/usr/bin/env bash

check_binaries() {
    if ! command -v systemctl &> /dev/null; then
        log "${ERR}[RUN] [ERROR] systemd (systemctl) is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
