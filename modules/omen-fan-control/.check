#!/usr/bin/env bash

check_binaries() {
    if ! command -v omen-fan-control &> /dev/null; then
        log "${ERR}[RUN] [ERROR] omen-fan-control is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
