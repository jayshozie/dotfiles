#!/usr/bin/env bash

check_binaries() {
    if ! command -v start-hyprland &> /dev/null; then
        log "${ERR}[RUN] [ERROR] start-hyprland is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
