#!/usr/bin/env bash

check_binaries() {
    if ! command -v keyd &> /dev/null; then
        log "${ERR}[RUN] [ERROR] keyd is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
