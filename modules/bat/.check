#!/usr/bin/env bash

check_binaries() {
    if ! command -v bat &> /dev/null; then
        log "${ERR}[RUN] [ERROR] bat is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
