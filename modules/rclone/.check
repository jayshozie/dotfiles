#!/usr/bin/env bash

check_binaries() {
    if ! command -v rclone &> /dev/null; then
        log "${ERR}[RUN] [ERROR] rclone is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
