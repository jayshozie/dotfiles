#!/usr/bin/env bash

check_binaries() {
    if ! command -v alacritty &> /dev/null; then
        log "${ERR}[RUN] [ERROR] alacritty is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
