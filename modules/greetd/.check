#!/usr/bin/env bash

check_binaries() {
    if ! command -v greetd &> /dev/null; then
        log "${ERR}[RUN] [ERROR] greetd is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
