#!/usr/bin/env bash

check_binaries() {
    if ! command -v zathura &> /dev/null; then
        log "${ERR}[RUN] [ERROR] zathura is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
