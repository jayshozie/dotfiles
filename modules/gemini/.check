#!/usr/bin/env bash

check_binaries() {
    if ! command -v gemini &> /dev/null; then
        log "${ERR}[RUN] [ERROR] gemini CLI is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
