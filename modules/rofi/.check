#!/usr/bin/env bash

check_binaries() {
    if ! command -v rofi &> /dev/null; then
        log "${ERR}[RUN] [ERROR] rofi is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
