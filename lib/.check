#!/usr/bin/env bash

check_binaries() {
    if ! command -v BINARY_NAME &> /dev/null; then
        log "${ERR}[RUN] [ERROR] BINARY_NAME is not installed!${RESET}"
        exit 1
    fi
}

check_binaries "$@"

exit 0
